# SmartSpend AI Workspace

The MERN application lives in `smartspend-ai/`.

```bash
cd smartspend-ai
npm run install-all
npm run dev
```

Backend: `smartspend-ai/server`

Frontend: `smartspend-ai/client`
